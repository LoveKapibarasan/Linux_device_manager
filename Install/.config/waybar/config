{
    "layer": "top",
    "position": "top",
    "height": 30,
    
    "modules-left": ["hyprland/workspaces"],
    "modules-center": ["hyprland/window"],
    "modules-right": ["cpu", "memory", "pulseaudio", "clock"],
    
    "hyprland/workspaces": {
        "format": "{name}"
    },
    
    "clock": {
        "format": "{:%Y-%m-%d %H:%M:%S}" 
    },
    
    "cpu": {
        "format": "CPU: {usage}% | Load: {load}",
        "tooltip": true,
        "tooltip-format": "Core 0: {max_frequency}GHz\nUsage: {usage}%"
    },
    
    "memory": {
        "format": "RAM: {used:.1f}G / {total:.1f}G"
    },
    
    "pulseaudio": {
        "format": "🔊 {volume}%",
        "format-muted": "🔇 Muted",
        "scroll-step": 5,
        "on-click": "pavucontrol"
    }
}
